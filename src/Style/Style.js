import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#635bff",
    },
    secondary: {
      main: "#635bff",
    },
  },
  typography: {
    fontFamily: "Inter",
  },
});
export default theme;
